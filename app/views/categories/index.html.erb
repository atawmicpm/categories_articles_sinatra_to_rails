<% @categories.each do |category| %>

<p><%= link_to category.name, category %></p>

<ul class="article-list">
	<% category.articles.each do |article| %>
	
	<li>
		<%= link_to article.title, [category, article] %>
	</li>
	
	<% end %>
</ul>

<% end %>